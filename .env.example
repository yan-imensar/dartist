# Default PocketBase backend URL (overridable in Settings).
PUBLIC_DEFAULT_POCKETBASE_URL=

# OAuth2 provider name as configured in PocketBase admin (Auth providers).
# Use "oidc", "oidc2" or "oidc3" depending on which OIDC slot PocketID occupies.
PUBLIC_OAUTH_PROVIDER=oidc
